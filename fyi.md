<!-- Generated by fyi::use_fyi_md() on 2026-01-10 -->
<!-- Regenerate with: fyi::use_fyi_md("ttsapi") -->

# fyi: ttsapi

## Exported Functions (ttsapi::)

| Function | Arguments |
|----------|-----------|
| `chatterbox_available` | port, timeout |
| `chatterbox_voice_upload` | voice_file, voice_name, port, timeout |
| `elevenlabs_voice_delete` | voice_id |
| `elevenlabs_voice_upload` | files, name, description, remove_background_noise, labels |
| `elevenlabs_voices` |  |
| `languages` |  |
| `set_elevenlabs_key` | api_key |
| `set_tts_base` | url |
| `set_tts_key` | key |
| `speech` | input, voice, file, backend, model, temperature, speed, exaggeration, cfg_weight, stability, similarity_boost, seed, response_format, instructions |
| `speech_clone` | input, voice_file, file, exaggeration, temperature, cfg_weight, speed, seed |
| `tts_health` |  |
| `tts_voices` | provider, base_url, timeout |
| `voice_upload` | voice_file, voice_name, language |
| `voices` |  |


## Internal Functions (ttsapi:::)

| Function | Arguments |
|----------|-----------|
| `.apply_speed_ffmpeg` | input_file, output_file, speed |
| `.fetch_voices_from_api` | base_url, timeout |
| `.gpuctl_acquire` |  |
| `.gpuctl_enabled` |  |
| `.gpuctl_register_service` |  |
| `.onLoad` | libname, pkgname |
| `.try_health_endpoint` | base, endpoint, timeout |
| `.tts_elevenlabs` | input, voice_id, file, model, stability, similarity_boost |
| `.tts_get` | endpoint |
| `.tts_get_api_base` |  |
| `.tts_get_api_key` |  |
| `.tts_post_json` | endpoint, body, expect_binary |
| `.tts_request` | endpoint, method, body, expect_binary |


## Options (ttsapi)

| Option | File | Type |
|--------|------|------|
| `op_ttsapi` | zzz.R | set |
| `ttsapi.api_base` | set_tts_base.R | get |
| `ttsapi.api_base` | set_tts_base.R | set |
| `ttsapi.api_base` | speech.R | get |
| `ttsapi.api_base` | speech.R | set |
| `ttsapi.api_key` | set_tts_key.R | get |
| `ttsapi.api_key` | set_tts_key.R | set |
| `ttsapi.api_key` | speech.R | get |
| `ttsapi.api_key` | speech.R | set |
| `ttsapi.base` | tts_providers.R | get |
| `ttsapi.elevenlabs_key` | elevenlabs_voice.R | get |
| `ttsapi.elevenlabs_key` | set_elevenlabs_key.R | set |
| `ttsapi.elevenlabs_key` | speech.R | get |
| `ttsapi.gpuctl` | gpuctl_integration.R | get |
| `ttsapi.gpuctl` | gpuctl_integration.R | set |
| `ttsapi.timeout` | internal_request.R | get |
| `ttsapi.timeout` | speech_clone.R | get |
| `ttsapi.timeout` | speech.R | get |
| `ttsapi.timeout` | tts_health.R | get |
| `ttsapi.timeout` | voice_upload.R | get |


# Documentation: ttsapi

## chatterbox_available

*Check if Chatterbox Service is Available*

```
_C_h_e_c_k _i_f _C_h_a_t_t_e_r_b_o_x _S_e_r_v_i_c_e _i_s _A_v_a_i_l_a_b_l_e

_D_e_s_c_r_i_p_t_i_o_n:

     Quick check if the local Chatterbox TTS API is reachable. Returns
     TRUE/FALSE without throwing errors.

_U_s_a_g_e:

     chatterbox_available(port, timeout)
     
_A_r_g_u_m_e_n_t_s:

    port: Port to check (default from CHATTERBOX_PORT env var or 4123)

 timeout: Timeout in seconds (default 2)

_V_a_l_u_e:

     TRUE if service is available, FALSE otherwise

_E_x_a_m_p_l_e_s:

     ## Not run:
     
       if (chatterbox_available()) {
         speech("Hello", voice = "default", backend = "chatterbox")
       }
     ## End(Not run)
     
```

## chatterbox_voice_upload

*Upload Voice to Chatterbox*

```
_U_p_l_o_a_d _V_o_i_c_e _t_o _C_h_a_t_t_e_r_b_o_x

_D_e_s_c_r_i_p_t_i_o_n:

     Uploads a voice sample directly to a local Chatterbox instance.
     Does not require setting the API base URL first.

_U_s_a_g_e:

     chatterbox_voice_upload(voice_file, voice_name, port, timeout)
     
_A_r_g_u_m_e_n_t_s:

voice_file: Character. Path to the voice sample file (mp3, wav).

voice_name: Character. Name to save the voice as.

    port: Port for Chatterbox API (default from CHATTERBOX_PORT env var
          or 4123)

 timeout: Timeout in seconds (default 30)

_V_a_l_u_e:

     TRUE if upload succeeded, FALSE otherwise. Does not throw errors.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
       # Upload a voice sample
       success <- chatterbox_voice_upload("my_voice.wav", "my-voice")
     
       if (success) {
         speech("Hello!", voice = "my-voice", backend = "chatterbox")
       }
     ## End(Not run)
     
```

## dot-apply_speed_ffmpeg

*Apply speed adjustment using ffmpeg*

```
_A_p_p_l_y _s_p_e_e_d _a_d_j_u_s_t_m_e_n_t _u_s_i_n_g _f_f_m_p_e_g

_D_e_s_c_r_i_p_t_i_o_n:

     Apply speed adjustment using ffmpeg

_U_s_a_g_e:

     .apply_speed_ffmpeg(input_file, output_file, speed)
     
```

## dot-fetch_voices_from_api

*Fetch voices from API*

```
_F_e_t_c_h _v_o_i_c_e_s _f_r_o_m _A_P_I

_D_e_s_c_r_i_p_t_i_o_n:

     Fetch voices from API

_U_s_a_g_e:

     .fetch_voices_from_api(base_url, timeout)
     
```

## dot-onLoad

*.onLoad*

```
._o_n_L_o_a_d

_D_e_s_c_r_i_p_t_i_o_n:

     .onLoad

_U_s_a_g_e:

     .onLoad(libname, pkgname)
     
```

## dot-try_health_endpoint

*Try a single health endpoint*

```
_T_r_y _a _s_i_n_g_l_e _h_e_a_l_t_h _e_n_d_p_o_i_n_t

_D_e_s_c_r_i_p_t_i_o_n:

     Try a single health endpoint

_U_s_a_g_e:

     .try_health_endpoint(base, endpoint, timeout)
     
```

## dot-tts_elevenlabs

*ElevenLabs TTS backend*

```
_E_l_e_v_e_n_L_a_b_s _T_T_S _b_a_c_k_e_n_d

_D_e_s_c_r_i_p_t_i_o_n:

     ElevenLabs TTS backend

_U_s_a_g_e:

     .tts_elevenlabs(input, voice_id, file, model, stability, similarity_boost)
     
```

## dot-tts_get_api_base

*Get the TTS API Base URL*

```
_G_e_t _t_h_e _T_T_S _A_P_I _B_a_s_e _U_R_L

_D_e_s_c_r_i_p_t_i_o_n:

     Get the TTS API Base URL

_U_s_a_g_e:

     .tts_get_api_base()
     
_V_a_l_u_e:

     Character string with the API base URL.

```

## dot-tts_get_api_key

*Get the TTS API Key*

```
_G_e_t _t_h_e _T_T_S _A_P_I _K_e_y

_D_e_s_c_r_i_p_t_i_o_n:

     Get the TTS API Key

_U_s_a_g_e:

     .tts_get_api_key()
     
_V_a_l_u_e:

     Character string with the API key, or NULL if not set.

```

## dot-tts_get

*GET from the TTS API*

```
_G_E_T _f_r_o_m _t_h_e _T_T_S _A_P_I

_D_e_s_c_r_i_p_t_i_o_n:

     GET from the TTS API

_U_s_a_g_e:

     .tts_get(endpoint)
     
_A_r_g_u_m_e_n_t_s:

endpoint: Character. The API endpoint.

_V_a_l_u_e:

     Parsed JSON response.

```

## dot-tts_post_json

*POST JSON to the TTS API*

```
_P_O_S_T _J_S_O_N _t_o _t_h_e _T_T_S _A_P_I

_D_e_s_c_r_i_p_t_i_o_n:

     POST JSON to the TTS API

_U_s_a_g_e:

     .tts_post_json(endpoint, body, expect_binary)
     
_A_r_g_u_m_e_n_t_s:

endpoint: Character. The API endpoint.

    body: List. Request body.

expect_binary: Logical. If TRUE, expect binary response.

_V_a_l_u_e:

     Response content.

```

## dot-tts_request

*Make an HTTP Request to the TTS API*

```
_M_a_k_e _a_n _H_T_T_P _R_e_q_u_e_s_t _t_o _t_h_e _T_T_S _A_P_I

_D_e_s_c_r_i_p_t_i_o_n:

     Low-level HTTP wrapper around curl::curl_fetch_memory().

_U_s_a_g_e:

     .tts_request(endpoint, method, body, expect_binary)
     
_A_r_g_u_m_e_n_t_s:

endpoint: Character. The API endpoint (e.g., "/v1/audio/speech").

  method: Character. HTTP method ("GET" or "POST").

    body: List or NULL. Request body for POST requests.

expect_binary: Logical. If TRUE, expect binary response (audio data).

_V_a_l_u_e:

     Response content (raw bytes if binary, parsed JSON otherwise).

```

## elevenlabs_voice_delete

*Delete ElevenLabs Voice*

```
_D_e_l_e_t_e _E_l_e_v_e_n_L_a_b_s _V_o_i_c_e

_D_e_s_c_r_i_p_t_i_o_n:

     Delete a voice from your ElevenLabs account.

_U_s_a_g_e:

     elevenlabs_voice_delete(voice_id)
     
_A_r_g_u_m_e_n_t_s:

voice_id: Character. The voice ID to delete.

_V_a_l_u_e:

     Invisible TRUE on success.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     elevenlabs_voice_delete("abc123")
     ## End(Not run)
     
```

## elevenlabs_voice_upload

*Upload Voice to ElevenLabs (Instant Voice Clone)*

```
_U_p_l_o_a_d _V_o_i_c_e _t_o _E_l_e_v_e_n_L_a_b_s (_I_n_s_t_a_n_t _V_o_i_c_e _C_l_o_n_e)

_D_e_s_c_r_i_p_t_i_o_n:

     Create an instant voice clone on ElevenLabs from audio samples.

_U_s_a_g_e:

     elevenlabs_voice_upload(files, name, description, remove_background_noise, labels)
     
_A_r_g_u_m_e_n_t_s:

   files: Character vector. Paths to audio files (1-25 files, each up
          to 10MB).

    name: Character. Name for the cloned voice.

description: Character or NULL. Optional description.

remove_background_noise: Logical. Remove background noise from samples.
          Default FALSE.

  labels: Named list or NULL. Optional labels (e.g., list(accent =
          "british")).

_V_a_l_u_e:

     List with voice_id and name on success.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     # Clone from a single file
     voice <- elevenlabs_voice_upload(
       files = "my_voice.mp3",
       name = "My Clone"
     )
     
     # Use the cloned voice
     speech("Hello!", voice = voice$voice_id, backend = "elevenlabs")
     
     # Clone from multiple samples
     voice <- elevenlabs_voice_upload(
       files = c("sample1.mp3", "sample2.mp3"),
       name = "Better Clone",
       remove_background_noise = TRUE
     )
     ## End(Not run)
     
```

## elevenlabs_voices

*List ElevenLabs Voices*

```
_L_i_s_t _E_l_e_v_e_n_L_a_b_s _V_o_i_c_e_s

_D_e_s_c_r_i_p_t_i_o_n:

     Get all voices available in your ElevenLabs account.

_U_s_a_g_e:

     elevenlabs_voices()
     
_V_a_l_u_e:

     Data frame with voice_id, name, and category columns.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     voices <- elevenlabs_voices()
     print(voices)
     ## End(Not run)
     
```

## languages

*List Supported Languages*

```
_L_i_s_t _S_u_p_p_o_r_t_e_d _L_a_n_g_u_a_g_e_s

_D_e_s_c_r_i_p_t_i_o_n:

     Returns a list of languages supported by the TTS server.

_U_s_a_g_e:

     languages()
     
_V_a_l_u_e:

     A character vector or list of supported language codes.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_tts_base("http://localhost:4123")
     langs <- languages()
     print(langs)
     ## End(Not run)
     
```

## set_elevenlabs_key

*Set ElevenLabs API Key*

```
_S_e_t _E_l_e_v_e_n_L_a_b_s _A_P_I _K_e_y

_D_e_s_c_r_i_p_t_i_o_n:

     Configure the API key for ElevenLabs TTS backend.

_U_s_a_g_e:

     set_elevenlabs_key(api_key)
     
_A_r_g_u_m_e_n_t_s:

 api_key: Character. Your ElevenLabs API key.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_elevenlabs_key("your-api-key-here")
     # Or use environment variable ELEVENLABS_API_KEY
     ## End(Not run)
     
```

## set_tts_base

*Set the TTS API Base URL*

```
_S_e_t _t_h_e _T_T_S _A_P_I _B_a_s_e _U_R_L

_D_e_s_c_r_i_p_t_i_o_n:

     Sets the base URL for the TTS API. This should be the root URL
     without trailing slash (e.g., "http://localhost:4123" or
     "https://api.openai.com").

_U_s_a_g_e:

     set_tts_base(url)
     
_A_r_g_u_m_e_n_t_s:

     url: Character string. The base URL for the API.

_V_a_l_u_e:

     Invisibly returns the previous value.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     # For local Chatterbox server
     set_tts_base("http://localhost:4123")
     
     # For OpenAI
     set_tts_base("https://api.openai.com")
     ## End(Not run)
     
```

## set_tts_key

*Set the TTS API Key*

```
_S_e_t _t_h_e _T_T_S _A_P_I _K_e_y

_D_e_s_c_r_i_p_t_i_o_n:

     Sets the API key for authentication. Required for cloud services
     like OpenAI, typically ignored by local servers.

_U_s_a_g_e:

     set_tts_key(key)
     
_A_r_g_u_m_e_n_t_s:

     key: Character string. The API key.

_V_a_l_u_e:

     Invisibly returns the previous value.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_tts_key(Sys.getenv("OPENAI_API_KEY"))
     ## End(Not run)
     
```

## speech_clone

*Generate Speech with Voice Cloning*

```
_G_e_n_e_r_a_t_e _S_p_e_e_c_h _w_i_t_h _V_o_i_c_e _C_l_o_n_i_n_g

_D_e_s_c_r_i_p_t_i_o_n:

     Uploads a voice sample and generates speech in that voice using
     the /v1/audio/speech/upload endpoint.

_U_s_a_g_e:

     speech_clone(input, voice_file, file, exaggeration, temperature, cfg_weight, speed, seed)
     
_A_r_g_u_m_e_n_t_s:

   input: Character. The text to convert to speech.

voice_file: Character. Path to the voice sample file (mp3, wav, etc.).

    file: Character or NULL. Output file path. If NULL, returns raw
          bytes.

exaggeration: Numeric or NULL. Exaggeration parameter
          (Chatterbox-specific).

temperature: Numeric or NULL. Sampling temperature for generation.

cfg_weight: Numeric or NULL. CFG weight parameter
          (Chatterbox-specific).

   speed: Numeric or NULL. Speed multiplier for the audio.

    seed: Integer or NULL. Random seed for reproducible output.

_V_a_l_u_e:

     If \code{file} is provided, invisibly returns the file path.  If
     \code{file} is NULL, returns raw audio bytes.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_tts_base("http://localhost:4123")
     
     # Clone voice and generate speech
     speech_clone(
       input = "Hello with my custom voice!",
       voice_file = "my_voice.mp3",
       file = "output.wav",
       exaggeration = 0.8
     )
     ## End(Not run)
     
```

## speech

*Generate Speech from Text*

```
_G_e_n_e_r_a_t_e _S_p_e_e_c_h _f_r_o_m _T_e_x_t

_D_e_s_c_r_i_p_t_i_o_n:

     Generate audio from text using various TTS backends: local
     Chatterbox server, OpenAI TTS API, or ElevenLabs API.

_U_s_a_g_e:

     speech(input, voice, file, backend, model, temperature, speed, exaggeration, cfg_weight, stability, similarity_boost, seed, response_format, instructions)
     
_A_r_g_u_m_e_n_t_s:

   input: Character. The text to convert to speech.

   voice: Character. The voice to use for synthesis. For OpenAI:
          "alloy", "echo", "fable", "onyx", "nova", "shimmer". For
          ElevenLabs: voice ID (e.g., "XpDLYThV0yUAFjVTok7m"). For
          Chatterbox: custom voice names uploaded via voice_upload().

    file: Character or NULL. Output file path. If NULL, returns raw
          bytes.

 backend: Character. Backend to use: "chatterbox" for local server,
          "openai" for OpenAI TTS API, "elevenlabs" for ElevenLabs API,
          or "auto" to use configured API base.

   model: Character or NULL. The model to use. For OpenAI: "tts-1" or
          "tts-1-hd". For ElevenLabs: "eleven_multilingual_v2"
          (default), "eleven_turbo_v2_5", etc. For Chatterbox:
          optional, often ignored.

temperature: Numeric or NULL. Sampling temperature for generation.

   speed: Numeric or NULL. Speed multiplier for the audio.

exaggeration: Numeric or NULL. Exaggeration parameter
          (Chatterbox-specific).

cfg_weight: Numeric or NULL. CFG weight parameter
          (Chatterbox-specific).

stability: Numeric or NULL. Voice stability 0-1 (ElevenLabs-specific).
          Default 0.5.

similarity_boost: Numeric or NULL. Similarity boost 0-1
          (ElevenLabs-specific). Default 0.75.

    seed: Integer or NULL. Random seed for reproducible output.

response_format: Character or NULL. Audio format (e.g., "wav", "mp3").
          If NULL and file is provided, inferred from file extension.

instructions: Character or NULL. Instructions for how the voice should
          speak (OpenAI-specific, e.g., "Speak in a cheerful and
          positive tone.").

_V_a_l_u_e:

     If \code{file} is provided, invisibly returns the file path.  If
     \code{file} is NULL, returns raw audio bytes.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     # Using local Chatterbox server
     set_tts_base("http://localhost:4123")
     speech("Hello, world!", voice = "FatherChristmas", file = "hello.wav")
     
     # Using OpenAI TTS
     speech("Hello, world!", voice = "nova", file = "hello.mp3", backend = "openai")
     
     # Using ElevenLabs
     speech("Hello, world!", voice = "XpDLYThV0yUAFjVTok7m",
            file = "hello.mp3", backend = "elevenlabs")
     ## End(Not run)
     
```

## tts_health

*Check TTS API Health*

```
_C_h_e_c_k _T_T_S _A_P_I _H_e_a_l_t_h

_D_e_s_c_r_i_p_t_i_o_n:

     Checks whether the TTS backend is reachable by trying common
     health endpoints.

_U_s_a_g_e:

     tts_health()
     
_V_a_l_u_e:

     A list with components: \describe{ \item{ok}{Logical. TRUE if the
     server is reachable.} \item{status}{Character. A human-readable
     status message.} \item{raw}{The raw response from the server (if
     any).} }

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_tts_base("http://localhost:4123")
     h <- tts_health()
     if (h$ok) {
       message("Server is ready!")
     }
     ## End(Not run)
     
```

## tts_providers

*TTS Provider Configurations*

```
_T_T_S _P_r_o_v_i_d_e_r _C_o_n_f_i_g_u_r_a_t_i_o_n_s

_D_e_s_c_r_i_p_t_i_o_n:

     A list of known TTS providers with their default voices and
     configuration.

_D_e_t_a_i_l_s:

     A named list where each element contains: \describe{
     \item{voices}{Character vector of default/known voice names, or
     NULL if dynamic} \item{env_var}{Name of environment variable for
     API key, or NULL if not needed} \item{base_url}{Default API base
     URL, or NULL} }

_E_x_a_m_p_l_e_s:

     names(tts_providers)
     tts_providers[["OpenAI"]]$voices
     
```

## tts_voices

*Get Voices for a TTS Provider*

```
_G_e_t _V_o_i_c_e_s _f_o_r _a _T_T_S _P_r_o_v_i_d_e_r

_D_e_s_c_r_i_p_t_i_o_n:

     Returns available voices for a given provider. Attempts to fetch
     dynamically from the API first, falls back to static list if
     unavailable.

_U_s_a_g_e:

     tts_voices(provider, base_url, timeout)
     
_A_r_g_u_m_e_n_t_s:

provider: Character string naming the provider (e.g., "OpenAI",
          "Chatterbox (Local)")

base_url: Optional base URL override for the provider's API

 timeout: Timeout in seconds for API requests (default 2)

_V_a_l_u_e:

     Character vector of voice names

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     tts_voices("OpenAI")
     tts_voices("Chatterbox (Local)")
     ## End(Not run)
     
```

## voice_upload

*Upload Voice to Library*

```
_U_p_l_o_a_d _V_o_i_c_e _t_o _L_i_b_r_a_r_y

_D_e_s_c_r_i_p_t_i_o_n:

     Uploads a voice sample to the server's voice library for reuse.
     Once uploaded, the voice can be used by name in speech().

_U_s_a_g_e:

     voice_upload(voice_file, voice_name, language)
     
_A_r_g_u_m_e_n_t_s:

voice_file: Character. Path to the voice sample file (mp3, wav, etc.).

voice_name: Character. Name to save the voice as.

language: Character or NULL. Language code (e.g., "en", "fr", "es").

_V_a_l_u_e:

     Invisibly returns the response from the server.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_tts_base("http://localhost:4123")
     
     # Upload a voice
     voice_upload(
       voice_file = "my_voice.wav",
       voice_name = "my-custom-voice"
     )
     
     # Upload with language
     voice_upload(
       voice_file = "french_voice.wav",
       voice_name = "french-speaker",
       language = "fr"
     )
     
     # Then use it by name
     speech(
       input = "Hello with my custom voice!",
       voice = "my-custom-voice",
       file = "output.wav"
     )
     ## End(Not run)
     
```

## voices

*List Available Voices*

```
_L_i_s_t _A_v_a_i_l_a_b_l_e _V_o_i_c_e_s

_D_e_s_c_r_i_p_t_i_o_n:

     Returns a list of available voices from the TTS server. Tries the
     /v1/audio/voices endpoint first, then falls back to /voices.

_U_s_a_g_e:

     voices()
     
_V_a_l_u_e:

     A character vector of voice names, or a list/data.frame depending
     on the server's response format.

_E_x_a_m_p_l_e_s:

     ## Not run:
     
     set_tts_base("http://localhost:4123")
     v <- voices()
     print(v)
     ## End(Not run)
     
```

