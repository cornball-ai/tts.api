<!-- Generated by fyi::use_fyi_md() on 2026-01-23 -->
<!-- Regenerate with: fyi::use_fyi_md("tts.api") -->

# fyi: tts.api

## Exported Functions (tts.api::)

| Function | Arguments |
|----------|-----------|
| `chatterbox_available` | port, timeout |
| `chatterbox_voice_upload` | voice_file, voice_name, port, timeout |
| `elevenlabs_voice_delete` | voice_id |
| `elevenlabs_voice_upload` | files, name, description, remove_background_noise, labels |
| `elevenlabs_voices` |  |
| `languages` |  |
| `set_elevenlabs_key` | api_key |
| `set_tts_base` | url |
| `set_tts_key` | key |
| `speech` | input, voice, file, backend, model, temperature, speed, exaggeration, cfg_weight, stability, similarity_boost, seed, response_format, instructions |
| `speech_clone` | input, voice_file, file, exaggeration, temperature, cfg_weight, speed, seed |
| `speech_design` | input, voice_description, file, language |
| `tts_health` |  |
| `tts_voices` | provider, base_url, timeout |
| `voice_upload` | voice_file, voice_name, language |
| `voices` |  |


## Internal Functions (tts.api:::)

| Function | Arguments |
|----------|-----------|
| `.apply_speed_ffmpeg` | input_file, output_file, speed |
| `.elevenlabs_api_key` |  |
| `.elevenlabs_request` | endpoint, method, handle |
| `.fetch_voices_from_api` | base_url, timeout |
| `.gpuctl_acquire` |  |
| `.gpuctl_enabled` |  |
| `.gpuctl_register_service` |  |
| `.onLoad` | libname, pkgname |
| `.try_health_endpoint` | base, endpoint, timeout |
| `.tts_elevenlabs` | input, voice_id, file, model, stability, similarity_boost |
| `.tts_fal` | input, voice, file, model |
| `.tts_get` | endpoint |
| `.tts_get_api_base` |  |
| `.tts_get_api_key` |  |
| `.tts_post_json` | endpoint, body, expect_binary |
| `.tts_request` | endpoint, method, body, expect_binary |


## Options (tts.api)

| Option | File | Type |
|--------|------|------|
| `op_tts.api` | zzz.R | set |
| `tts.api_base` | set_tts_base.R | get |
| `tts.api_base` | set_tts_base.R | set |
| `tts.api_base` | speech.R | get |
| `tts.api_base` | speech.R | set |
| `tts.api_key` | set_tts_key.R | get |
| `tts.api_key` | set_tts_key.R | set |
| `tts.api_key` | speech.R | get |
| `tts.api_key` | speech.R | set |
| `tts.base` | tts_providers.R | get |
| `tts.elevenlabs_key` | internal_request.R | get |
| `tts.elevenlabs_key` | set_elevenlabs_key.R | set |
| `tts.elevenlabs_key` | speech.R | get |
| `tts.gpuctl` | gpuctl_integration.R | get |
| `tts.gpuctl` | gpuctl_integration.R | set |
| `tts.timeout` | internal_request.R | get |
| `tts.timeout` | speech_clone.R | get |
| `tts.timeout` | speech.R | get |
| `tts.timeout` | tts_health.R | get |
| `tts.timeout` | voice_upload.R | get |


# Documentation: tts.api

## chatterbox_available

### Check if Chatterbox Service is Available

#### Description

Quick check if the local Chatterbox TTS API is reachable.
Returns TRUE/FALSE without throwing errors.

#### Usage

```r
chatterbox_available(port = NULL, timeout = 2)
```

#### Arguments

- **`port`**: Port to check (default from CHATTERBOX_PORT env var or 4123)
- **`timeout`**: Timeout in seconds (default 2)

#### Value

TRUE if service is available, FALSE otherwise

#### Examples

```r
  if (chatterbox_available()) {
    speech("Hello", voice = "default", backend = "chatterbox")
  }
```


## chatterbox_voice_upload

### Upload Voice to Chatterbox

#### Description

Uploads a voice sample directly to a local Chatterbox instance.
Does not require setting the API base URL first.

#### Usage

```r
chatterbox_voice_upload(voice_file, voice_name, port = NULL, timeout = 30)
```

#### Arguments

- **`voice_file`**: Character. Path to the voice sample file (mp3, wav).
- **`voice_name`**: Character. Name to save the voice as.
- **`port`**: Port for Chatterbox API (default from CHATTERBOX_PORT env var or 4123)
- **`timeout`**: Timeout in seconds (default 30)

#### Value

TRUE if upload succeeded, FALSE otherwise. Does not throw errors.

#### Examples

```r
  # Upload a voice sample
  success <- chatterbox_voice_upload("my_voice.wav", "my-voice")

  if (success) {
    speech("Hello!", voice = "my-voice", backend = "chatterbox")
  }
```


## dot-apply_speed_ffmpeg

### Apply speed adjustment using ffmpeg

#### Description

Apply speed adjustment using ffmpeg

#### Usage

```r
.apply_speed_ffmpeg(input_file, output_file, speed)
```


## dot-elevenlabs_api_key

### Get ElevenLabs API Key

#### Description

Get ElevenLabs API Key

#### Usage

```r
.elevenlabs_api_key()
```

#### Value

Character. The API key.


## dot-elevenlabs_request

### Make an HTTP Request to the ElevenLabs API

#### Description

Make an HTTP Request to the ElevenLabs API

#### Usage

```r
.elevenlabs_request(endpoint, method = "GET", handle = NULL)
```

#### Arguments

- **`endpoint`**: Character. The API endpoint (e.g., "voices").
- **`method`**: Character. HTTP method ("GET", "POST", or "DELETE").
- **`handle`**: curl handle or NULL. Pre-configured handle for multipart forms.

#### Value

curl response object.


## dot-fetch_voices_from_api

### Fetch voices from API

#### Description

Fetch voices from API

#### Usage

```r
.fetch_voices_from_api(base_url, timeout = 2)
```


## dot-try_health_endpoint

### Try a single health endpoint

#### Description

Try a single health endpoint

#### Usage

```r
.try_health_endpoint(base, endpoint, timeout)
```


## dot-tts_elevenlabs

### ElevenLabs TTS backend

#### Description

ElevenLabs TTS backend

#### Usage

```r
.tts_elevenlabs(
  input,
  voice_id,
  file = NULL,
  model = NULL,
  stability = NULL,
  similarity_boost = NULL
)
```


## dot-tts_fal

### fal.ai TTS backend

#### Description

fal.ai TTS backend

#### Usage

```r
.tts_fal(input, voice = NULL, file = NULL, model = NULL)
```


## dot-tts_get_api_base

### Get the TTS API Base URL

#### Description

Get the TTS API Base URL

#### Usage

```r
.tts_get_api_base()
```

#### Value

Character string with the API base URL.


## dot-tts_get_api_key

### Get the TTS API Key

#### Description

Get the TTS API Key

#### Usage

```r
.tts_get_api_key()
```

#### Value

Character string with the API key, or NULL if not set.


## dot-tts_get

### GET from the TTS API

#### Description

GET from the TTS API

#### Usage

```r
.tts_get(endpoint)
```

#### Arguments

- **`endpoint`**: Character. The API endpoint.

#### Value

Parsed JSON response.


## dot-tts_post_json

### POST JSON to the TTS API

#### Description

POST JSON to the TTS API

#### Usage

```r
.tts_post_json(endpoint, body, expect_binary = FALSE)
```

#### Arguments

- **`endpoint`**: Character. The API endpoint.
- **`body`**: List. Request body.
- **`expect_binary`**: Logical. If TRUE, expect binary response.

#### Value

Response content.


## dot-tts_request

### Make an HTTP Request to the TTS API

#### Description

Low-level HTTP wrapper around curl::curl_fetch_memory().

#### Usage

```r
.tts_request(endpoint, method = "GET", body = NULL, expect_binary = FALSE)
```

#### Arguments

- **`endpoint`**: Character. The API endpoint (e.g., "/v1/audio/speech").
- **`method`**: Character. HTTP method ("GET" or "POST").
- **`body`**: List or NULL. Request body for POST requests.
- **`expect_binary`**: Logical. If TRUE, expect binary response (audio data).

#### Value

Response content (raw bytes if binary, parsed JSON otherwise).


## elevenlabs_voice_delete

### Delete ElevenLabs Voice

#### Description

Delete a voice from your ElevenLabs account.

#### Usage

```r
elevenlabs_voice_delete(voice_id)
```

#### Arguments

- **`voice_id`**: Character. The voice ID to delete.

#### Value

Invisible TRUE on success.

#### Examples

```r
elevenlabs_voice_delete("abc123")
```


## elevenlabs_voice_upload

### Upload Voice to ElevenLabs (Instant Voice Clone)

#### Description

Create an instant voice clone on ElevenLabs from audio samples.

#### Usage

```r
elevenlabs_voice_upload(
  files,
  name,
  description = NULL,
  remove_background_noise = FALSE,
  labels = NULL
)
```

#### Arguments

- **`files`**: Character vector. Paths to audio files (1-25 files, each up to 10MB).
- **`name`**: Character. Name for the cloned voice.
- **`description`**: Character or NULL. Optional description.
- **`remove_background_noise`**: Logical. Remove background noise from samples. Default FALSE.
- **`labels`**: Named list or NULL. Optional labels (e.g., list(accent = "british")).

#### Value

List with voice_id and name on success.

#### Examples

```r
# Clone from a single file
voice <- elevenlabs_voice_upload(
  files = "my_voice.mp3",
  name = "My Clone"
)

# Use the cloned voice
speech("Hello!", voice = voice$voice_id, backend = "elevenlabs")

# Clone from multiple samples
voice <- elevenlabs_voice_upload(
  files = c("sample1.mp3", "sample2.mp3"),
  name = "Better Clone",
  remove_background_noise = TRUE
)
```


## elevenlabs_voices

### List ElevenLabs Voices

#### Description

Get all voices available in your ElevenLabs account.

#### Usage

```r
elevenlabs_voices()
```

#### Value

Data frame with voice_id, name, and category columns.

#### Examples

```r
voices <- elevenlabs_voices()
print(voices)
```


## languages

### List Supported Languages

#### Description

Returns a list of languages supported by the TTS server.

#### Usage

```r
languages()
```

#### Value

A character vector or list of supported language codes.

#### Examples

```r
set_tts_base("http://localhost:4123")
langs <- languages()
print(langs)
```


## set_elevenlabs_key

### Set ElevenLabs API Key

#### Description

Configure the API key for ElevenLabs TTS backend.

#### Usage

```r
set_elevenlabs_key(api_key)
```

#### Arguments

- **`api_key`**: Character. Your ElevenLabs API key.

#### Examples

```r
set_elevenlabs_key("your-api-key-here")
# Or use environment variable ELEVENLABS_API_KEY
```


## set_tts_base

### Set the TTS API Base URL

#### Description

Sets the base URL for the TTS API. This should be the root URL without
trailing slash (e.g., "http://localhost:4123" or "https://api.openai.com").

#### Usage

```r
set_tts_base(url)
```

#### Arguments

- **`url`**: Character string. The base URL for the API.

#### Value

Invisibly returns the previous value.

#### Examples

```r
# For local Chatterbox server
set_tts_base("http://localhost:4123")

# For OpenAI
set_tts_base("https://api.openai.com")
```


## set_tts_key

### Set the TTS API Key

#### Description

Sets the API key for authentication. Required for cloud services like OpenAI,
typically ignored by local servers.

#### Usage

```r
set_tts_key(key)
```

#### Arguments

- **`key`**: Character string. The API key.

#### Value

Invisibly returns the previous value.

#### Examples

```r
set_tts_key(Sys.getenv("OPENAI_API_KEY"))
```


## speech_clone

### Generate Speech with Voice Cloning

#### Description

Uploads a voice sample and generates speech in that voice using the
/v1/audio/speech/upload endpoint.

#### Usage

```r
speech_clone(
  input,
  voice_file,
  file = NULL,
  exaggeration = NULL,
  temperature = NULL,
  cfg_weight = NULL,
  speed = NULL,
  seed = NULL
)
```

#### Arguments

- **`input`**: Character. The text to convert to speech.
- **`voice_file`**: Character. Path to the voice sample file (mp3, wav, etc.).
- **`file`**: Character or NULL. Output file path. If NULL, returns raw bytes.
- **`exaggeration`**: Numeric or NULL. Exaggeration parameter (Chatterbox-specific).
- **`temperature`**: Numeric or NULL. Sampling temperature for generation.
- **`cfg_weight`**: Numeric or NULL. CFG weight parameter (Chatterbox-specific).
- **`speed`**: Numeric or NULL. Speed multiplier for the audio.
- **`seed`**: Integer or NULL. Random seed for reproducible output.

#### Value

If `file` is provided, invisibly returns the file path.
  If `file` is NULL, returns raw audio bytes.

#### Examples

```r
set_tts_base("http://localhost:4123")

# Clone voice and generate speech
speech_clone(
  input = "Hello with my custom voice!",
  voice_file = "my_voice.mp3",
  file = "output.wav",
  exaggeration = 0.8
)
```


## speech_design

### Generate Speech with Designed Voice

#### Description

Create a voice from a natural language description and generate speech.
This endpoint requires the qwen3-tts-api backend with the voice design model.

#### Usage

```r
speech_design(input, voice_description, file = NULL, language = "English")
```

#### Arguments

- **`input`**: Character. The text to convert to speech.
- **`voice_description`**: Character. Natural language description of the
desired voice (e.g., "A warm, friendly female voice with a slight British accent").
- **`file`**: Character or NULL. Output file path. If NULL, returns raw bytes.
- **`language`**: Character. Language for synthesis. Supported: Chinese, English,
Japanese, Korean, German, French, Russian, Portuguese, Spanish, Italian.

#### Details

This function uses the `/v1/audio/speech/design` endpoint which is
specific to qwen3-tts-api. It requires the VoiceDesign model to be loaded
(loaded on first use).

#### Value

If `file` is provided, invisibly returns the file path.
  If `file` is NULL, returns raw audio bytes.

#### Examples

```r
set_tts_base("http://localhost:4123")

# Generate speech with a custom designed voice
speech_design(
  input = "Hello, I am your AI assistant.",
  voice_description = "A professional male voice, clear and authoritative",
  file = "assistant.wav"
)

# Playful voice
speech_design(
  input = "Let's have some fun!",
  voice_description = "An energetic young female voice, cheerful and playful",
  file = "playful.wav"
)
```


## speech

### Generate Speech from Text

#### Description

Generate audio from text using various TTS backends: local Chatterbox server,
OpenAI TTS API, or ElevenLabs API.

#### Usage

```r
speech(
  input,
  voice,
  file = NULL,
  backend = c("auto", "chatterbox", "openai", "elevenlabs", "fal"),
  model = NULL,
  temperature = NULL,
  speed = NULL,
  exaggeration = NULL,
  cfg_weight = NULL,
  stability = NULL,
  similarity_boost = NULL,
  seed = NULL,
  response_format = NULL,
  instructions = NULL
)
```

#### Arguments

- **`input`**: Character. The text to convert to speech.
- **`voice`**: Character. The voice to use for synthesis.
For OpenAI: "alloy", "echo", "fable", "onyx", "nova", "shimmer".
For ElevenLabs: voice ID (e.g., "XpDLYThV0yUAFjVTok7m").
For Chatterbox: custom voice names uploaded via voice_upload().
- **`file`**: Character or NULL. Output file path. If NULL, returns raw bytes.
- **`backend`**: Character. Backend to use: "chatterbox" for local server,
"openai" for OpenAI TTS API, "elevenlabs" for ElevenLabs API,
"fal" for fal.ai TTS models, or "auto" to use configured API base.
- **`model`**: Character or NULL. The model to use.
For OpenAI: "tts-1" or "tts-1-hd".
For ElevenLabs: "eleven_multilingual_v2" (default), "eleven_turbo_v2_5", etc.
For Chatterbox: optional, often ignored.
- **`temperature`**: Numeric or NULL. Sampling temperature for generation.
- **`speed`**: Numeric or NULL. Speed multiplier for the audio.
- **`exaggeration`**: Numeric or NULL. Exaggeration parameter (Chatterbox-specific).
- **`cfg_weight`**: Numeric or NULL. CFG weight parameter (Chatterbox-specific).
- **`stability`**: Numeric or NULL. Voice stability 0-1 (ElevenLabs-specific). Default 0.5.
- **`similarity_boost`**: Numeric or NULL. Similarity boost 0-1 (ElevenLabs-specific). Default 0.75.
- **`seed`**: Integer or NULL. Random seed for reproducible output.
- **`response_format`**: Character or NULL. Audio format (e.g., "wav", "mp3").
If NULL and file is provided, inferred from file extension.
- **`instructions`**: Character or NULL. Instructions for how the voice should
speak (OpenAI-specific, e.g., "Speak in a cheerful and positive tone.").

#### Value

If `file` is provided, invisibly returns the file path.
  If `file` is NULL, returns raw audio bytes.

#### Examples

```r
# Using local Chatterbox server
set_tts_base("http://localhost:4123")
speech("Hello, world!", voice = "FatherChristmas", file = "hello.wav")

# Using OpenAI TTS
speech("Hello, world!", voice = "nova", file = "hello.mp3", backend = "openai")

# Using ElevenLabs
speech("Hello, world!", voice = "XpDLYThV0yUAFjVTok7m",
       file = "hello.mp3", backend = "elevenlabs")
```


## tts_health

### Check TTS API Health

#### Description

Checks whether the TTS backend is reachable by trying common health endpoints.

#### Usage

```r
tts_health()
```

#### Value

A list with components:
- **ok**: Logical. TRUE if the server is reachable.
- **status**: Character. A human-readable status message.
- **raw**: The raw response from the server (if any).

#### Examples

```r
set_tts_base("http://localhost:4123")
h <- tts_health()
if (h$ok) {
  message("Server is ready!")
}
```


## tts_providers

### TTS Provider Configurations

#### Description

A list of known TTS providers with their default voices and configuration.

#### Usage

```r
tts_providers
```

#### Details

A named list where each element contains:
- **voices**: Character vector of default/known voice names, or NULL if dynamic
- **env_var**: Name of environment variable for API key, or NULL if not needed
- **base_url**: Default API base URL, or NULL

#### Examples

```r
names(tts_providers)
tts_providers[["OpenAI"]]$voices
```


## tts_voices

### Get Voices for a TTS Provider

#### Description

Returns available voices for a given provider. Attempts to fetch dynamically
from the API first, falls back to static list if unavailable.

#### Usage

```r
tts_voices(provider, base_url = NULL, timeout = 2)
```

#### Arguments

- **`provider`**: Character string naming the provider (e.g., "OpenAI", "Chatterbox (Local)")
- **`base_url`**: Optional base URL override for the provider's API
- **`timeout`**: Timeout in seconds for API requests (default 2)

#### Value

Character vector of voice names

#### Examples

```r
tts_voices("OpenAI")
tts_voices("Chatterbox (Local)")
```


## voice_upload

### Upload Voice to Library

#### Description

Uploads a voice sample to the server's voice library for reuse.
Once uploaded, the voice can be used by name in speech().

#### Usage

```r
voice_upload(voice_file, voice_name, language = NULL)
```

#### Arguments

- **`voice_file`**: Character. Path to the voice sample file (mp3, wav, etc.).
- **`voice_name`**: Character. Name to save the voice as.
- **`language`**: Character or NULL. Language code (e.g., "en", "fr", "es").

#### Value

Invisibly returns the response from the server.

#### Examples

```r
set_tts_base("http://localhost:4123")

# Upload a voice
voice_upload(
  voice_file = "my_voice.wav",
  voice_name = "my-custom-voice"
)

# Upload with language
voice_upload(
  voice_file = "french_voice.wav",
  voice_name = "french-speaker",
  language = "fr"
)

# Then use it by name
speech(
  input = "Hello with my custom voice!",
  voice = "my-custom-voice",
  file = "output.wav"
)
```


## voices

### List Available Voices

#### Description

Returns a list of available voices from the TTS server.
Tries the /v1/audio/voices endpoint first, then falls back to /voices.

#### Usage

```r
voices()
```

#### Value

A character vector of voice names, or a list/data.frame depending
  on the server's response format.

#### Examples

```r
set_tts_base("http://localhost:4123")
v <- voices()
print(v)
```


